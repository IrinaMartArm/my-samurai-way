import React from "react";
import { StyledHeader } from "./StyledHeader";

export const Header: React.FC = () => {
    return (  
        <StyledHeader>
            <h1>Hello</h1>
        </StyledHeader>
    );
}
